rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ RÈGLES DE DÉVELOPPEMENT - PERMISSIVES ⚠️
    // À SÉCURISER AVANT LA PRODUCTION !
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
